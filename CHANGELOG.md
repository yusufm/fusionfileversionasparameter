# Changelog

## 1.0.0

- Initial App Store submission.
- Creates a `version_num` user parameter (if missing) on document activation.
- Updates `version_num` right before save to match the version number the file will have after saving.
